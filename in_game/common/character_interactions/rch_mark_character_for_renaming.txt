rch_mark_character_for_renaming_interaction = {
	ai_tick = never
	message = no
	on_own_nation = yes
	potential = {
		always = yes
	}
	allow = {
		always = yes
	}
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "choose_character"
		column = {
			data = name
		}
		visible = {
			always = yes
		}
		enabled = {
			always = yes
		}
	}
	effect = {
		scope:actor = {
			set_variable = {
				name = rch_marked_character
				value = scope:recipient
			}
			dynasty = {
				rch_set_dynasty_to_be_renamed = yes
			}
		}
	}
}
